import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const LogginInfo = () => {
  return (
    <View>
      <Text>LogginInfo</Text>
    </View>
  )
}

export default LogginInfo

const styles = StyleSheet.create({})